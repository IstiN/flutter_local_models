id: qwen-image-2512-4bit
display_name: Qwen Image 2512 4bit
description: Balanced mflux Qwen Image bundle for local text-to-image generation on
  Apple Silicon.
runtime_adapter: mflux
tasks:
- image_generation
source:
  provider: huggingface
  repo: mlx-community/Qwen-Image-2512-4bit
  revision: main
  license: apache-2.0
packaging:
  release_tag: model-qwen-image-2512-4bit
  archive_name: qwen-image-2512-4bit.tar
  chunk_size_bytes: 1900000000
  asset_prefix: qwen-image-2512-4bit
requirements:
  platform: macos-apple-silicon
  min_memory_gb: 24
  recommended_memory_gb: 48
  notes:
  - mflux text-to-image runtime
  - Balanced Qwen Image tier
capabilities:
  audio_input: false
  audio_output: false
  tool_calling: false
runtime_config:
  default_parameters:
    width: 1024
    height: 1024
    steps: 30
    guidance: 4.0
  parameter_schema:
    type: object
    properties:
      width:
        type: integer
      height:
        type: integer
      steps:
        type: integer
      guidance:
        type: number
      seed:
        type: integer
  output:
    media_type: image/png
    extension: png
    default_size: 1024x1024
  extra:
    mflux_runner: qwen
model_card:
  summary: Balanced mflux Qwen Image bundle for local text-to-image generation on
    Apple Silicon.
  use_cases:
  - Local text-to-image generation
  - Prompt-to-image UX prototyping
  - Offline creative workflows
  limitations:
  - Generation is memory intensive
  - Image generation UI/runtime adapter is still experimental
  languages:
  - prompt-based
  tags:
  - image-generation
  - mflux
  - mlx
  links:
    huggingface: https://huggingface.co/mlx-community/Qwen-Image-2512-4bit
