name: local_models_sdk
description: Headless Dart SDK for local model storage, downloads, and runtime abstractions.
version: 0.1.0
repository: https://github.com/IstiN/flutter_local_models
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  crypto: ^3.0.7
  local_models_core:
    path: ../local_models_core
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
