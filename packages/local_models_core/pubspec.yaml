name: local_models_core
description: Pure Dart core types for local model manifests, registries, release plans, and runtime summaries.
version: 0.1.0
repository: https://github.com/IstiN/flutter_local_models

environment:
  sdk: ^3.10.8

dependencies:
  crypto: ^3.0.6
  meta: ^1.16.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
